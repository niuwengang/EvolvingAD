#include <fstream>
#include <iostream>
#include <string>
#include <unistd.h> //for get_current_dir_name

int main()
{
    // const std::string project_folder_path = get_current_dir_name();
    // const std::string config_file_path = project_folder_path + "/config/config.json";
    // std::ifstream ifs("package.json");
    // Json::Reader reader;
    // Json::Value obj;
    // reader.parse(ifs, obj);
    // std::cout << " name " << obj["name"].asString() << std::endl;
}